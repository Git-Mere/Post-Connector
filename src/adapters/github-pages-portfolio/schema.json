{
  "format": "json",
  "outputNote": "Output must be a single JSON object — no prose, no markdown, no code fences.",
  "fields": {
    "title": { "type": "string", "required": true },
    "tech": { "type": "string[]", "required": true },
    "description": { "type": "string", "required": true }
  }
}
